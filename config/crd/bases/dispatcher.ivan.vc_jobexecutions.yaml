---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: jobexecutions.dispatcher.ivan.vc
spec:
  group: dispatcher.ivan.vc
  names:
    kind: JobExecution
    listKind: JobExecutionList
    plural: jobexecutions
    singular: jobexecution
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: JobExecution is the Schema for the jobexecutions API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: JobExecutionSpec defines the desired state of JobExecution
            properties:
              jobTemplateName:
                description: The Job Definition to execute.
                type: string
            required:
            - jobTemplateName
            type: object
          status:
            description: JobExecutionStatus defines the observed state of JobExecution
            properties:
              job:
                type: string
              phase:
                description: 'INSERT ADDITIONAL STATUS FIELD - define observed state
                  of cluster Important: Run "make" to regenerate code after modifying
                  this file'
                type: string
            required:
            - job
            - phase
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
